---
title: day34
date: '2026-02-20 19:45:07'
updated: '2026-02-20 20:10:41'
permalink: /post/day34-z1vekta.html
comments: true
toc: true
---



# day34

# 第九章 动态规划part02

今天开始逐渐有 dp的感觉了，前 两题 不同路径，可以好好研究一下，适合进阶

 详细布置

### ** 62.不同路径 **

本题大家掌握动态规划的方法就可以。 数论方法 有点非主流，很难想到。

<u>[https://programmercarl.com/0062.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84.html](https://programmercarl.com/0062.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84.html)</u>

[视频讲解：]()​<u>[https://www.bilibili.com/video/BV1ve4y1x7Eu](https://www.bilibili.com/video/BV1ve4y1x7Eu)</u>

### ** 63. 不同路径 II **

<u>[https://programmercarl.com/0063.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84II.htmlhttps://programmercarl.com/0063.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84II.html](https://programmercarl.com/0063.%E4%B8%8D%E5%90%8C%E8%B7%AF%E5%BE%84II.html)</u>

[视频讲解：]()​<u>[https://www.bilibili.com/video/BV1Ld4y1k7c6](https://www.bilibili.com/video/BV1Ld4y1k7c6)</u>

### **343.  整数拆分 （可跳过）**

本题思路并不容易想，一刷建议可以跳过。如果学有余力，可以看视频理解一波。

<u>[https://programmercarl.com/0343.%E6%95%B4%E6%95%B0%E6%8B%86%E5%88%86.html](https://programmercarl.com/0343.%E6%95%B4%E6%95%B0%E6%8B%86%E5%88%86.html)</u>

[视频讲解：]()​<u>[https://www.bilibili.com/video/BV1Mg411q7YJ](https://www.bilibili.com/video/BV1Mg411q7YJ)</u>

### **96. .不同的二叉搜索树 （可跳过）**

本题思路并不容易想，一刷建议可以跳过。 如果学有余力，可以看视频理解一波。

<u>[https://programmercarl.com/0096.%E4%B8%8D%E5%90%8C%E7%9A%84%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91.html](https://programmercarl.com/0096.%E4%B8%8D%E5%90%8C%E7%9A%84%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91.html)</u>

[视频讲解：]()​<u>[https://www.bilibili.com/video/BV1eK411o7QA](https://www.bilibili.com/video/BV1eK411o7QA)</u>

### ** 62.不同路径 **

[62. 不同路径 - 力扣（LeetCode）](https://leetcode.cn/problems/unique-paths/solutions/3062432/liang-chong-fang-fa-dong-tai-gui-hua-zu-o5k32/)

```java
class Solution {
    public int uniquePaths(int m, int n) {
        int[][] memo = new int[m][n];
        return dfs(m - 1, n - 1, memo);
    }

    private int dfs(int i, int j, int[][] memo) {
        if (i < 0 || j < 0) {
            return 0;
        }
        if (i == 0 && j == 0) {
            return 1;
        }
        if (memo[i][j] != 0) { // 之前计算过
            return memo[i][j];
        }
        return memo[i][j] = dfs(i - 1, j, memo) + dfs(i, j - 1, memo);
    }
}

作者：灵茶山艾府
链接：https://leetcode.cn/problems/unique-paths/solutions/3062432/liang-chong-fang-fa-dong-tai-gui-hua-zu-o5k32/
来源：力扣（LeetCode）
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
```

- ### 核心逻辑拆解

  这段代码的核心思想是： **“如果一个格子我已经算过到达它的路径数，下次再问我，我就直接翻本子（**​**​`memo`​**​ **）告诉你，绝对不重新算。”**

  #### 1. 备忘录的初始化

  Java

  ```
  int[][] memo = new int[m][n];
  return dfs(m - 1, n - 1, memo);
  ```

  - 这里巧妙利用了 Java 的语言特性：`int`​ 数组在创建时，默认值全都是 ​**0**。
  - 因为题目限定了有效路径数至少是 1，所以这里的 0 就天然充当了\*\*“这个状态还没被计算过”\*\*的标记。
  - 从终点 `(m - 1, n - 1)` 开始往回倒推，这非常符合人类正常的“回溯”直觉。

  #### 2. 递归的终点（Base Case）

  Java

  ```
  if (i < 0 || j < 0) {
      return 0; // 越界了，说明这条路走不通，提供 0 条路径
  }
  if (i == 0 && j == 0) {
      return 1; // 成功回到了起点，恭喜你，找到了一条完整的合法路径！
  }
  ```

  这两步是所有 DFS 必备的终止条件，防止无限递归导致栈溢出（`StackOverflowError`）。

  #### 3. 灵魂优化：查表拦截

  Java

  ```
  if (memo[i][j] != 0) { // 之前计算过
      return memo[i][j];
  }
  ```

  这就是那句“稍微优化一下得分就更高”的具象化体现！如果没有这三行代码，这就是个纯暴力的 DFS，遇到稍大的棋盘就会因为存在海量的“重叠子问题”而超时（Time Limit Exceeded）。有了它，时间复杂度直接从指数级降维打击到了 $O(m \times n)$。

  #### 4. 状态转移与记录

  Java

  ```
  return memo[i][j] = dfs(i - 1, j, memo) + dfs(i, j - 1, memo);
  ```

  这行代码做了一石二鸟的操作：

  1. **计算：**  当前格子 `(i, j)`​ 的路径数 \= 从上方格子 `(i - 1, j)`​ 过来的路径数 + 从左方格子 `(i, j - 1)` 过来的路径数。
  2. **存表并返回：**  把算出来的结果存进 `memo[i][j]` 中，并且作为当前函数的返回值扔给上一层。

  ---

  ### 复杂度评估

  - **时间复杂度：**  $O(m \times n)$。因为有备忘录的存在，总共 $m \times n$ 个格子，每个格子最多只会被计算一次。
  - **空间复杂度：**  $O(m \times n)$。主要开销是备忘录数组 `memo` 的大小，以及递归调用栈的深度最大为 $O(m + n)$。

  **下一步建议：**

  你现在已经掌握了把 DFS 升级为记忆化搜索的模板。其实只要把这套代码稍微变个形，就能顺理成章地推导出真正的“自底向上”的 DP 数组写法。

  **需要我带你趁热打铁，看看怎么把这段代码转换成最标准的 DP 循环写法吗？**  或者你想用这个记忆化模板去秒杀另一道非常相似的题（比如“最小路径和”）？

### 63.不同路径 II

[63. 不同路径 II - 力扣（LeetCode）](https://leetcode.cn/problems/unique-paths-ii/description/)

```java
class Solution {
    public int uniquePathsWithObstacles(int[][] obstacleGrid) {
        int m = obstacleGrid.length;
        int n = obstacleGrid[0].length;
        int[][] memo = new int[m][n];
        for (int[] row : memo) {
            Arrays.fill(row, -1); // -1 表示没有计算过
        }
        return dfs(m - 1, n - 1, obstacleGrid, memo);
    }

    private int dfs(int i, int j, int[][] obstacleGrid, int[][] memo) {
        if (i < 0 || j < 0 || obstacleGrid[i][j] == 1) {
            return 0;
        }
        if (i == 0 && j == 0) {
            return 1;
        }
        if (memo[i][j] != -1) { // 之前计算过
            return memo[i][j];
        }
        return memo[i][j] = dfs(i - 1, j, obstacleGrid, memo) + dfs(i, j - 1, obstacleGrid, memo);
    }
}

作者：灵茶山艾府
链接：https://leetcode.cn/problems/unique-paths-ii/solutions/3059862/jiao-ni-yi-bu-bu-si-kao-dpcong-ji-yi-hua-451i/
来源：力扣（LeetCode）
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
```

这是一段非常经典的 **动态规划 (Dynamic Programming, DP)**  解法，用于解决 LeetCode 上的“不同路径 II”问题。你分享的代码采用了​**自顶向下的记忆化搜索 (Top-Down Memoization)** ，这种方法非常符合直觉，是连接暴力递归和优化后 DP 表格之间的完美桥梁。

下面我为你详细拆解这段代码的运作原理。

---

### 代码逻辑拆解

目标是在避开障碍物（标记为 `1`​）的情况下，找到从左上角 `(0, 0)`​ 到右下角 `(m-1, n-1)` 的所有不同路径。

1. ​**状态转移方程 (核心逻辑)** ​：  
   要到达格子 `(i, j)`​，你只能从它上方的格子 `(i-1, j)`​ 或左侧的格子 `(i, j-1)` 走过来。因此，到达当前格子的路径总数等于到达这两个相邻格子的路径数之和：

   $$
   paths(i, j) = paths(i-1, j) + paths(i, j-1)
   $$

- ​**边界条件 (递归的“刹车”限制)** ：

  - ​**越界**：如果 $i < 0$ 或 $j < 0$，说明走到了网格外面，这是无效路径，返回 `0`。
  - ​**遇到障碍**​：如果 `obstacleGrid[i][j] == 1`​，说明此路不通，返回 `0`。
  - ​**到达起点**：如果 $i == 0$ 且 $j == 0$，说明成功倒推回了起点，找到了一条有效路径，返回 `1`。
- ​**记忆化 (提升效率的法宝)** ​：  
  如果没有 `memo`​ 数组，代码会一次又一次地重复计算同一个格子的路径数，导致时间复杂度呈指数级爆炸。通过将计算过的结果存入 `memo[i][j]`​ 中（初始化为 `-1`​ 来区分“未计算”和“路径数为 0”的情况），可以确保每个格子​**只被计算一次**，从而将时间复杂度稳稳降至 $O(m \times n)$。

---

### 对比：自顶向下 vs 自底向上

虽然这种递归写法可读性极强，但在面试中，面试官往往会要求你将其转换为\*\*迭代（自底向上）\*\*写法，以避免在巨大网格下可能出现的递归栈溢出 (Stack Overflow) 问题。

|**特性**|**自顶向下 (你分享的代码)**|**自底向上 (迭代法)**||||
| --| -------------------------| ------------------------| --| --| --|
|**推导方向**|递归：从终点倒推回起点|循环：从起点顺推至终点||||
|**空间复杂度**|$O(m \times n) +$递归调用栈开销|$O(m \times n)$(可进一步优化)||||
|**代码可读性**|极高 (直接翻译数学公式)|中等||||

---

### 💡 进阶：空间优化

目前的解法使用了 $O(m \times n)$ 的额外空间来存储 `memo`​。实际上，如果我们采用自底向上的迭代法，由于当前行的状态只依赖于**上一行**和**当前行左侧**的格子，我们完全可以压缩状态，只使用一个​**一维数组**，将空间复杂度骤降至 $O(n)$。

**需要我为你展示如何将这段代码重构为空间复杂度仅为** **$O(n)$** **的迭代版本吗？**

### **343.  整数拆分 （可跳过）**

[343. 整数拆分 - 力扣（LeetCode）](https://leetcode.cn/problems/integer-break/description/)

‍

# 96.不同的二叉搜索树

[96. 不同的二叉搜索树 - 力扣（LeetCode）](https://leetcode.cn/problems/unique-binary-search-trees/description/)
